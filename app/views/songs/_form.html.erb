<%= form_for @song  do |f| %>
  <%= f.label :artist_id %>
  <%= current_artist(@song) %>
  <%= artist_select(@song) %><br>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.submit %>
<% end %>
